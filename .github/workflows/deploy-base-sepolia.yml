name: Deploy to Base Sepolia

on:
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy Contracts
    runs-on: ubuntu-latest
    environment: base-sepolia

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: false

      - name: Install Foundry
        uses: foundry-rs/foundry-toolchain@v1
        with:
          version: nightly

      - name: Install dependencies
        run: |
          forge install foundry-rs/forge-std --no-git
          forge install OpenZeppelin/openzeppelin-contracts-upgradeable@v5.0.2 --no-git
          forge install OpenZeppelin/openzeppelin-contracts@v5.0.2 --no-git
          cd lib/openzeppelin-contracts-upgradeable && git submodule update --init --recursive && cd ../..
          cd lib/openzeppelin-contracts && git submodule update --init --recursive && cd ../..

      - name: Deploy contracts
        env:
          PRIVATE_KEY: ${{ secrets.DEPLOYER_PRIVATE_KEY }}
          BASE_SEPOLIA_RPC_URL: ${{ secrets.BASE_SEPOLIA_RPC_URL }}
          BASESCAN_API_KEY: ${{ secrets.BASESCAN_API_KEY }}
        run: |
          forge script script/Deploy.s.sol:DeployScript \
            --rpc-url $BASE_SEPOLIA_RPC_URL \
            --broadcast \
            --verify \
            -vvvv

      - name: Save deployment artifacts
        uses: actions/upload-artifact@v4
        with:
          name: deployment-artifacts
          path: broadcast/
